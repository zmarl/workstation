# workstation — 新しい Ubuntu PC の作業環境づくり（AI 向けの規則）

このリポジトリは、今の Windows PC（2026-09-17 時点）の道具・アプリ・AI の設定を、新しい Ubuntu 26.04 PC へ再現するためのキットです。セットアップ当日は Claude Code（または Codex）がこの規則に従って進め、オーナーはパスワード入力・ログイン・再起動・了承だけを行います。

オーナーは技術者ではありません。やり取りは日本語の敬語（です・ます調）で行います。内部名やコマンドを並べるのではなく、「今どこまで進んだか・なぜ・次にオーナーが何をするか」を自然な文章で伝えます。

## 最初にやること（毎回）

1. `bash setup/status.sh` で進み具合を確認し、終わっていない段から続けます。再起動やログインし直しの後もここから再開します。
2. `NOTES.md` の「当日の記録」を読み、前回の続きの判断事項がないか確認します。

## 進める順番

| 段 | 実行 | 誰の出番か |
| --- | --- | --- |
| 1 | `setup/run-as-admin.sh 10-base` → `setup/wait-admin.sh 10-base` | オーナーがパスワード |
| 2 | `setup/run-as-admin.sh 20-gpu` → 待つ → 再起動が要ると出たらオーナーに再起動を依頼 → 再起動後に `20-gpu` をもう一度流し、`~/.local/state/workstation/reboot-required` を消す | パスワード・再起動 |
| 3 | `setup/run-as-admin.sh 30-apps` → 待つ → 公式の gh（`/usr/bin/gh`）が入ったら、最初に仮置きした `~/.local/bin/gh` を消す（ログイン情報は `~/.config/gh` に残る） | パスワード |
| 4 | 権限の変更を 1 項目ずつ説明し、了承を得たものだけ `setup/run-as-admin.sh 35-permissions <項目...>` | 了承・パスワード・ログインし直し |
| 5 | `bash setup/40-user-tools.sh` | なし |
| 6 | `bash setup/50-ai-config.sh` | なし（プラグイン導入に Claude のログインが必要） |
| 7 | `bash setup/60-dotfiles-repos.sh` | なし（GitHub ログイン済みが前提） |
| 8 | 各アプリのログインを 1 つずつ案内 | ログイン |
| 9 | `bash setup/90-verify.sh` → 自動確認の失敗を直す → 目視確認をオーナーに依頼 | 目視確認 |
| 10 | 結果を `NOTES.md` に書き、このリポジトリへ commit / push | なし |

失敗した項目は、ログ（`~/.local/state/workstation/logs/`）で原因を確かめ、直してから**その項目だけ**を流し直します（`30-apps` は `run-as-admin.sh 30-apps chrome discord` のように項目名を渡せます）。同じ失敗を理由なく繰り返しません。

## 守ること

- **パスワードを入力しない。** 管理者操作は必ず `setup/run-as-admin.sh` でウィンドウを開き、オーナーが入力します。`sudo` を自分で実行する、sudo の設定（sudoers）を変える、パスワードを尋ねて受け取ることはしません。
- **管理者操作の前に、何が変わるかを日本語で説明します。** 各スクリプト冒頭の `# CHANGES:` がその材料です。
- **権限に関わる変更は項目ごとに了承を取ります。** `kvm`（Cowork）、`docker`（管理者と同等の強さ）、`input`（音声入力がキー入力を注入できる）、`ssh`（外から入れる）。まとめて了承を取りません。
- **再起動・ログアウトはオーナーが行います。** 必要になったら理由と、戻ったら `status.sh` から再開することを伝えます。
- **旧ドライブ（旧 4TB・旧 1TB）には触りません。** マウント・読み書き・フォーマットは、この作業の後に移行計画 第 4 版 §4.4 の 6 番以降として別に行います。DB の復元、`.env` の配置、CUDA や vLLM など投資アプリ側の作業もこのキットの範囲外です（Linux 移行の凍結の対象）。
- **ログイン情報・秘密情報を扱いません。** トークンやパスワードをファイル・ログ・コミット・チャットに書きません。MCP の雛形はトークン抜きです。
- **想定外のことが起きたら止まります。** 起きたこと・原因の見立て・選択肢を `NOTES.md` に書き、オーナーに説明して判断を仰ぎます。
- 台本を直したら、そのまま commit します（コミットメッセージは英語、`git add` はパスを明示）。push はこの作業の記録として行ってよい範囲です。force push はしません。

## 構成

| 場所 | 中身 |
| --- | --- |
| `docs/day1-owner-sheet.md` | オーナーが当日に読む 1 枚（段階 1 と最初に Claude へ貼る文） |
| `docs/decisions.md` | 技術的な選択とその理由（ドライバの版、WezTerm、Chrome の入れ方など） |
| `inventory/` | Windows のアプリ棚卸しと Linux での扱い |
| `setup/` | セットアップ台本（何度流しても同じ結果になる） |
| `packages/` | apt で入れるパッケージの一覧 |
| `ai-config/` | Claude Code と Codex の設定・メモリ（秘密情報は含まない） |
| `dotfiles/` | WezTerm・starship・fish・bash の設定 |
| `tests/` | 旧 PC で行う予行演習（ubuntu:26.04 コンテナ） |
| `NOTES.md` | 判断待ちの事項と当日の記録 |
